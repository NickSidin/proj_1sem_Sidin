// задаем значение переменной x 
let x = 5;

// вычисляем значение функции 
let y = (x + 1) / 3 + Math.pow(x + 1, 2);

// выводим значение функции
console.log("Значение функции при x = " + x + ": " + y);

// значение функции при x = 5: 38